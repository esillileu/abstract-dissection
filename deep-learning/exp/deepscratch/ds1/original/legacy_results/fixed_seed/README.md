# Legacy fixed-seed archive

The retired `exp run ds1 --original` cache is archived in this directory on
the working machine. Its large generated artifacts remain git-ignored and must
not be interpreted as schema-v1 seed trials or included in MLflow analysis.
