test:
    make -C reference test

check:
    make -C reference test

sanitize:
    make -C reference sanitize

profile-smoke:
    uv run python profiling/profile.py smoke

profile *args:
    uv run python profiling/profile.py run {{args}}
